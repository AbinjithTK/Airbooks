// Placeholder for Phase 2 — the 3D library will live here. For now the library
// view reuses the existing HTML bookshelf overlay, so this renders nothing
// extra into the scene.
export function LibraryScene() {
  return null;
}
